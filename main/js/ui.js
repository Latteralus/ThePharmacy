/**
 * @fileoverview This file defines the UI class, which is responsible for
 * managing the user interface of the game. It handles updating various UI
 * elements and rendering information to the user.
 */

class UI {
    constructor() {
    }

    updateTime() {
        console.log("[UI] Updating Time");
    }
}

window.ui = new UI();